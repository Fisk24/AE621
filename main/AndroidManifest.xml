<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fisk.ae621">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBarBlue">
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBarBlue">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".PostViewActivity"
            android:label="@string/title_activity_post_view"
            android:theme="@style/NoActionBarBlue" />
        <activity android:name=".DevDataModelViewerActivity" />
        <activity android:name=".CommentIndexActivity" />
        <activity
            android:name=".PoolActivity"
            android:label="@string/title_activity_pool"
            android:theme="@style/NoActionBarBlue" />
        <activity
            android:name=".PostActivity"
            android:label="@string/title_activity_post_index"
            android:theme="@style/NoActionBarBlue"></activity>
    </application>

</manifest>